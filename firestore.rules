rules_version = '2';
// Open rules: portfolio analytics/contact only — no sensitive data.
// Anyone can read/write; deploy with: firebase deploy --only firestore:rules
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
