# Firebase Configuration
REACT_APP_FIREBASE_API_KEY=your_firebase_api_key
REACT_APP_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your_project_id
REACT_APP_FIREBASE_STORAGE_BUCKET=your_project_id.firebasestorage.app
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
REACT_APP_FIREBASE_APP_ID=your_app_id


# Analytics Settings
REACT_APP_ENABLE_ANALYTICS=true
# Optional: higher ipinfo.io limits (free tier at https://ipinfo.io/signup). Works without it via geojs.io fallback.
REACT_APP_IPINFO_TOKEN=

# Traffic email notifications (Worker → Resend)
# From: traffic@humza-butt.space | Default To: humzab1711@hotmail.com (hardcoded)
# Extra recipients managed in Traffic → Email notifications (Firestore)
#
# Keep these two identical — client sends Bearer token, Worker checks NOTIFY_SECRET:
NOTIFY_SECRET=
REACT_APP_TRAFFIC_NOTIFY_SECRET=
#
# Resend API key (Worker only — never prefix with REACT_APP_)
RESEND_API_KEY=
#
# Sync Worker secrets to Cloudflare (and optionally rebuild/deploy):
#   npm run secrets:sync
#   npm run secrets:sync:deploy
#   npm run secrets:sync -- --dry-run
#   npm run secrets:sync -- --dev-vars
#
# Note: REACT_APP_* is baked in at `npm run build`. Uploading Worker secrets alone
# will not fix "Missing REACT_APP_TRAFFIC_NOTIFY_SECRET" in production — you must redeploy.